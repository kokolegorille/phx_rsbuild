{
  "name": "phx_rsbuild",
  "private": true,
  "description": "Phoenix 1.7 and Rsbuild",
  "main": "src/js/app.js",
  "version": "1.0.0",
  "scripts": {
    "watch": "rsbuild build --watch",
    "deploy": "rsbuild build",
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.6.9",
    "@rsbuild/plugin-react": "^0.6.9"
  }
}